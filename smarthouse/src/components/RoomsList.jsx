import React from 'react'
// import Room from './Room'

export default function RoomsList(props) {
  return (
    <div>
        {/* {props.roomsArr.map((val,i)=>{
          return <Room roomName={val.roomName} roomType={val.roomType} roomColor={val.roomColor} index={i} />
        })} */}
    </div>
  )
}
